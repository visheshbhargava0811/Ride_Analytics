# Ride_Analytics
Real-time + batch lakehouse on Azure — FastAPI events → Event Hub → Databricks medallion architecture (Bronze/Silver/Gold) with Star Schema &amp; SCD Type 2 via Structured Streaming CDC
